# =============================================================================
#  env.production.example — Variables de entorno para producción
#  IMPORTANTE: Copiar como .env.production y configurar antes de desplegar
#  Generar secrets: openssl rand -base64 32
# =============================================================================

APP_VERSION=v1.0.0
CORS_ORIGIN=https://licitaciones.cl

# PostgreSQL
POSTGRES_DB=licitaciones_chile
POSTGRES_USER=licitaciones_user
POSTGRES_PASSWORD=REEMPLAZAR_PASSWORD_SEGURO

# Redis
REDIS_PASSWORD=REEMPLAZAR_REDIS_PASSWORD

# Elasticsearch
ELASTICSEARCH_PASSWORD=REEMPLAZAR_ES_PASSWORD

# JWT y Encriptación (openssl rand -base64 64)
JWT_SECRET=REEMPLAZAR_SECRET_256_BITS
ENCRYPTION_KEY=REEMPLAZAR_AES_256_KEY_32_BYTES

# Grafana
GRAFANA_ADMIN_PASSWORD=REEMPLAZAR_GRAFANA_PASSWORD
GRAFANA_DOMAIN=grafana.licitaciones.cl

# Frontend
VITE_API_BASE_URL=https://licitaciones.cl/api/v1
DOCKER_REGISTRY=ghcr.io/tu-organizacion

# Portales — configurar credenciales
CHILECOMPRA_TICKET=
WHEREX_API_KEY=
WHEREX_API_URL=https://api.wherex.cl/v1
SAP_ARIBA_CLIENT_ID=
SAP_ARIBA_CLIENT_SECRET=
SAP_ARIBA_REALM=
COUPA_INSTANCE_URL=
COUPA_CLIENT_ID=
COUPA_CLIENT_SECRET=
SICEP_API_KEY=
PORTAL_MINERO_USER=
PORTAL_MINERO_PASSWORD=

# Email (SendGrid recomendado)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASSWORD=REEMPLAZAR_SENDGRID_API_KEY
SMTP_FROM=noreply@licitaciones.cl
